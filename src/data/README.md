# Data Processing Module

Handles dataset loading, preprocessing, sequence construction, and minority class augmentation.

## Components

- `dataset_loader.py`: Core dataset class implementations
- `preprocessor.py`: Feature normalization and encoding
- `temporal_augmentation.py`: Synthetic minority oversampling for sequences
- `batch_sampler.py`: Stratified batch construction